/**
 * Legacy dropdown debugging script.
 *
 * Intentionally left as a no-op to avoid breaking imports.
 * If you need to re-enable verbose dropdown debugging locally,
 * you can temporarily add logging here behind a feature flag.
 */
